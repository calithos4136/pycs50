def hello(name="world"):
    return f"Hello, {name}."


def goodbye(name="world"):
    return f"Goodbye, {name}."
# __name__ or __main__
# using the two underscores limits the variables or functions
# to only be run when in main or the file is run directly
